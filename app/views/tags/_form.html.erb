<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h3>Add Tag</h3>
  <div class="field">
    <label><strong>Tag:</strong></label><br> 
    <%= f.text_field :str %>
  </div>

  <br><br>
  <div class="actions">
    <%= f.button :submit, name: "subaction", value: the_id %>
  </div>
<br><br><br>
<% end %>
